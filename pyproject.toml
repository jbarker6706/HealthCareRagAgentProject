[project]
name = "HealthcareRAGAgentProject"
version = "6.7.1"
requires-python = ">=3.8"
dependencies = [
    "pandas",
    "langchain",
    "langchain_community",
    "langchain_core",
    "langchain_ollama",
    "langgraph"
]
